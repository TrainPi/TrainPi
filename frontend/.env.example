# Copy this file to .env.local and adjust as needed.

# false = call the real backend at NEXT_PUBLIC_API_URL. true/unset = use mock data.
NEXT_PUBLIC_USE_MOCK=false

# Backend base URL
NEXT_PUBLIC_API_URL=http://localhost:8000
